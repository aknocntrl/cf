#include<iostream>
using namespace std;
int main(){
    int t;
    cin>>t;
    while(t--){
        int n,i=0;
        cin>>n;
        string s;
        cin>>s;
        for(char &c:s){
            c=tolower(c);}
        if(s[i]!='m'){
            cout<<"NO"<<endl;
            continue;}
        while(i<n && s[i]=='m'){
            i++;}
        if(i==n || s[i]!='e'){
            cout<<"NO"<<endl;
            continue;}
        while(i<n && s[i]=='e'){
            i++;}
        if(i==n || s[i]!='o'){
            cout<<"NO"<<endl;
            continue;}
        while(i<n && s[i]=='o'){
            i++;}
        if(i==n || s[i]!='w'){
            cout<<"NO"<<endl;
            continue;}
        while(i<n && s[i]=='w'){
            i++;}
        if(i==n){
            cout<<"YES"<<endl;}
        else
        cout<<"NO"<<endl;}
    return 0;
}